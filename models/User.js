//var assert = require('assert');
/*const mongoose = require("mongoose");
const Schema = mongoose.Schema;
// Create Schema
const UserSchema = new Schema({
  name: {
    type: String,
    required: true
  },
  email: {
    type: String,
    required: true
  },
  password: {
    type: String,
    required: true
  },
  date: {
    type: Date,
    default: Date.now
  }
});
module.exports = User = mongoose.model("users", UserSchema);*/

/*var Document = require('camo').Document;
 
class User extends Document {
    constructor() {
        super();
 
        this.name = String;
        this.email = String
        this.password = String;
        this.Date = Date;


    }
 
    static collectionName() {
        return 'DatabaseTest';
    }
}*/

/*const createDocuments = function(db, callback) {
  
const collection = db.collection('DatabaseTest');

db.createUser(
   {
     user: String,
     pwd: String,
     email: String
     date: Date
     roles: [ ]
   }
), function(err, result) {
    console.log("Inserted a document into the collection");
    callback(result);

}*/


